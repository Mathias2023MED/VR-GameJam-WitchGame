using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

public class XRManagerGrab : MonoBehaviour
{
    /*private XRGrabInteractable grabInteractable;

    private void Awake()
    {
        // Get the XRGrabInteractable on this prefab
        grabInteractable = GetComponent<XRGrabInteractable>();
    }

    private void OnEnable()
    {
        // Find the XRInteractionManager in the scene
        XRInteractionManager interactionManager = FindObjectOfType<XRInteractionManager>();

        if (interactionManager != null)
        {
            // Assign it to the Grab Interactable
            grabInteractable.interactionManager = interactionManager;
        }
        else
        {
            Debug.LogError($"[{name}] No XRInteractionManager found in the scene! " +
                           "Please make sure one exists.");
        }
    }*/
}